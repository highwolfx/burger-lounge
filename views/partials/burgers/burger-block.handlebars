<li class="list-group-item">
	<div class="container">
	  <div class="row">
	    <div class="col-9">
	      <span class="align-middle">{{burger_name}}</span>
	    </div>
	    <div class="col">
	      	<button class="change-eaten btn btn-dark" data-id="{{id}}" data-devoured="{{devoured}}">
				{{#if devoured}}{{else}}DEVOUR{{/if}}
			</button>
	    </div>
	  </div>
	</div>
</li class="list-group-item">